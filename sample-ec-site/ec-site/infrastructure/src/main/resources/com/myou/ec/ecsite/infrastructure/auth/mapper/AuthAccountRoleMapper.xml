<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myou.ec.ecsite.infrastructure.auth.mapper.AuthAccountRoleMapper">

    <select id="selectRoleCodesByAccountId" resultType="string">
        SELECT r.role_code
        FROM AUTH_ACCOUNT_ROLE ar
                 JOIN AUTH_ROLE r
                      ON r.auth_role_id = ar.auth_role_id
        WHERE ar.auth_account_id = #{authAccountId}
        ORDER BY r.auth_role_id
    </select>

    <insert id="insert">
        INSERT INTO AUTH_ACCOUNT_ROLE (auth_account_id,
                                       auth_role_id,
                                       created_by)
        VALUES (#{authAccountId},
                #{authRoleId},
                #{createdBy})
    </insert>

    <delete id="delete">
        DELETE
        FROM AUTH_ACCOUNT_ROLE
        WHERE auth_account_id = #{authAccountId}
    </delete>

</mapper>
