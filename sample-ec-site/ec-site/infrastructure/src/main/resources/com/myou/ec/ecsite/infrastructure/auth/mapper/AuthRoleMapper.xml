<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myou.ec.ecsite.infrastructure.auth.mapper.AuthRoleMapper">

    <resultMap id="AuthRoleRecordMap" type="com.myou.ec.ecsite.infrastructure.auth.record.AuthRoleRecord">
        <id column="ROLE_CODE" property="roleCode" />
        <result column="ROLE_NAME" property="roleName" />
        <result column="DESCRIPTION" property="description" />
        <result column="CREATED_AT" property="createdAt" />
        <result column="CREATED_BY" property="createdBy" />
        <result column="UPDATED_AT" property="updatedAt" />
        <result column="UPDATED_BY" property="updatedBy" />
    </resultMap>

    <select id="findAll" resultMap="AuthRoleRecordMap">
        SELECT * FROM AUTH_ROLE ORDER BY SORT_ORDER
    </select>

    <select id="findRoleCodesByAccountId" resultType="string">
        SELECT ROLE_CODE FROM AUTH_ACCOUNT_ROLE WHERE AUTH_ACCOUNT_ID = #{authAccountId}
    </select>
    
    <delete id="deleteUserRoles">
        DELETE FROM AUTH_ACCOUNT_ROLE WHERE AUTH_ACCOUNT_ID = #{authAccountId}
    </delete>
    
    <insert id="insertUserRole">
        INSERT INTO AUTH_ACCOUNT_ROLE (AUTH_ACCOUNT_ID, ROLE_CODE, CREATED_AT, CREATED_BY)
        VALUES (#{authAccountId}, #{roleCode}, CURRENT_TIMESTAMP, 'SYSTEM')
    </insert>

</mapper>
